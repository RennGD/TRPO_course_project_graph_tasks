#include "all_path.h"
#include "connect.h"
#include "one_path.h"
#include <vector>

using namespace std;

int main()
{
    int a, b, one_vertex, one_edge;
    vector<vector<int>> A;
    vector<int> Min, Max;

    input(a, b, A);

    return 0;
}